# Basic Example

This example shows the minimal configuration required to use this module.

## Usage

```bash
cd examples/basic
terraform init
terraform plan
terraform apply
```

## Clean Up

```bash
terraform destroy
```
